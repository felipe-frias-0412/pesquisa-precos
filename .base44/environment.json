{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "ADMIN_API_KEY",
      "description": "Internal admin panel key. Set a long random string to protect the /admin panel. A development placeholder is generated automatically.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
